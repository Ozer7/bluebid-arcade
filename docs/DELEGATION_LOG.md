# Delegation log: BlueBid Arcade (Wave 1)

Oriel Zer · MCON 357 · Claude (claude.ai, agent mode)

| # | What I asked Claude to do | What it produced | How I verified it |
|---|---|---|---|
| 1 | Read the assignment and the Slack thread, and tell me the plan before building. | A plan: one static cabinet page, six canvas games plus Imitation as a Claude artifact (per Dr. Robinson's Slack answer), hosted at `games.bluebid.online` so the quiz stays on the main domain. | I read the plan against the requirements list and picked the subdomain option. |
| 2 | Build a shared engine and the cabinet menu. | `js/engine.js` (fixed 60 Hz loop, input, pause/result) and `js/cabinet.js` (game cards, a Human/Computer toggle for each side, a computer-vs-computer demo behind the menu). | Opened every game and switched every side between Human and Computer. |
| 3 | Snake: the human places apples and the computer steers. | Time-aware path search plus a "can I still reach my tail" safety check. Apple rules: must be reachable, at least 3 cells from the head, hunger sized from the real path length. | Ran 12 computer-vs-computer games: snake won 5, apples won 7. Games lasted 77–134 s. |
| 4 | Breakout: the computer plays against the human. | Head to head: two paddles, a shared brick wall, 5 lives each. The computer predicts the ball's path with bounces and has the same paddle speed cap as a human. | Played both paddles. Checked that the computer misses sometimes early and less later. |
| 5 | Splat: the human lays out the columns and the computer flies without cheating. | Flappy flier with a look-ahead that simulates "flap / don't flap" using the real physics and only sees columns already on screen. A builder can only move each gap so far. | The first version always crashed. Claude traced it to an off-by-one-frame look-ahead and fixed it. Final: 60 games, flier 62% / builder 38%, deaths spread across levels. |
| 6 | Asteroids: the computer flies and the human sends the asteroids. | A pilot that dodges by testing escape manoeuvres and leads its shots. The thrower spends energy that recharges faster every wave. | A test run found rocks hitting the ship before entering the screen (a wrap-around bug), and it was fixed. Final: 40 games, pilot 32% / thrower 68%. |
| 7 | Missile Command: design the flip myself. | Attacker vs defender, either side human. The attacker picks targets, and splitting warheads appear from wave 3. The computer defender solves the intercept point with a reaction delay and aim error. | The first version had the defender winning 100%. After tuning: 60 games, defender 65% / attacker 35%, often down to one city. |
| 8 | My own game with a flip. | **Stack & Deal**: Tetris where one side deals every piece. Dealer rules: no piece three times in a row, and an I-piece at least every 12. The computer stacker moves one key press at a time. | Found a bug where soft-drop carried over to the next piece. Final: 40 games, stacker 72% / dealer 28%. |
| 9 | Imitation: another human in a second browser or the AI, with no instant connection. | A Claude artifact using a shared store for matchmaking and Claude for the AI side. Every search waits a random 7–18 s, so the wait doesn't reveal human vs AI. | Claude checked that the shared matchmaking store is readable at the Contributor access level. *(I still need to: open it in two browser windows to test human vs human, then play alone to get Claude.)* |
| 10 | Test everything. | `tests/sim.js` (headless balance runs) and `tests/browser.mjs` (every game × 3 role combos in Chromium with screenshots and console errors). | Browser run: no page errors. The only console errors were Google Fonts being blocked in the sandbox. No horizontal scroll at phone width. |
| 11 | Deployment steps. | `netlify.toml`, README steps for GitHub → Netlify continuous deployment → `games.bluebid.online` CNAME. | *(fill in once done: the Netlify deploy log shows an auto-deploy from a push, and the site loads in a private window)* |

**What I can explain on request:** the engine loop (`engine.js`), how `api.isHuman(side)` switches each side between input and AI, and each game's AI in its own file, marked with `/* ---------- computer ... ---------- */` comments.
